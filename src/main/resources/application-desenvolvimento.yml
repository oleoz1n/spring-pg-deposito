spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/spring-pg-deposito-2tdspg?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: update
